import type { MapwizeConfiguration, OfflineManager } from './types';
export declare const createOfflineManager: (mapwizeConfiguration: MapwizeConfiguration) => OfflineManager;
export default createOfflineManager;
